# manifests/clusters/network/default-deny.yaml
# Cluster-wide default deny example (applied selectively via namespace labels or additional policies).

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: platform-default-deny
  namespace: platform-system
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
